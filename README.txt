# dots
dev environment for macos (WIP) - neovim, kitty (terminal emulater), zellij (local multiplexer config), aerospace (macOS window manager), zsh (z shell configs)

everything symlinked- to cleanup + install script eventually